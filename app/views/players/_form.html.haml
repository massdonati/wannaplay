= simple_form_for Player.new do |f|
  = hidden_field_tag :game_id, @game.id
  =f.input :name, :input_html => { :id=>'textarea',:class => "span4", :placeholder => 'Write your name'}
  = f.button :submit, :class => "btn btn-small", :value => 'Submit'